android {
    ...existing code...
    defaultConfig {
        ...existing code...
        minSdkVersion 21 // Ajuste conforme necessário
        targetSdkVersion 33
        ...existing code...
    }
    ...existing code...
}